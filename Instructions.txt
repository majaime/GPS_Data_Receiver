This code is designed for GPS device "BU-353".
The code finds the particular string "GPGSV" which contains "headeing" value.
In order to find other values such as latitude, longitude, etc. one needs to change that to other strings according to NMEA structure.
